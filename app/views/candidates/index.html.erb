<h1>Candidats</h1>

<% @statuses_ids.each do |status_id| %>
  <div><%= Status.find(status_id).description %></div>
  <% @candidates.where(status_id: status_id).each do |candidate| %>  
    <div><%= link_to "#{candidate.user.first_name} #{candidate.user.last_name}", candidate_path(candidate)%></div>
  <% end %>
<% end %>
<br>
<% if current_user.role.code != "CAN" && current_user.candidate.nil?%>
	<%= link_to 'Créer votre candidature', new_candidate_path, class: "btn btn-primary" %>	
<% elsif current_user.role.code != "CAN" && !current_user.candidate.nil?%>	
	<%= link_to 'Modifier votre candidature', edit_candidate_path, class: "btn btn-primary" %>	
<% end %>